import React from 'react';
import './FxPollsBar.scss';

const FxPollsBar = ({
  sellPercent = 0,
  buyPercent = 0,
}) => (
  <div className="fx-polls">
    <div className="fx-polls__labels">
      <div className="fx-polls__label fx-polls__label--sell">
        <span className="fx-polls__label-price">
          {`Selling ${sellPercent}%`}
        </span>
        <span className="fx-polls__label-currency">USD</span>
      </div>

      <div className="fx-polls__bar">
        <div
          className="fx-polls__bar-segment fx-polls__bar-segment--sell"
          style={{ width: `${sellPercent}%` }}
        />
        <div className="fx-polls__bar-segment fx-polls__bar-segment--gap" />
        <div
          className="fx-polls__bar-segment fx-polls__bar-segment--buy"
          style={{ width: `${buyPercent}%` }}
        />
      </div>

      <div className="fx-polls__label fx-polls__label--buy">
        <span className="fx-polls__label-price">
          {`${buyPercent}% Buying`}
        </span>
        <span className="fx-polls__label-currency">USD</span>
      </div>
    </div>
  </div>
);

export default FxPollsBar;

@import '~@scb/styles/css/index.scss';

.fx-polls {
  width: 100%;
  border-radius: 2rem;
  padding: 1rem;
  margin-bottom: 10rem;
  border: 2px solid transparent;
  /* gradient border + rounded corners via dual backgrounds */
  background-image:
    linear-gradient(#ffffff, #ffffff),
    linear-gradient(
      86.56deg,
      #00e14d 0.13%,
      #e3e3e3 4.89%,
      #e3e3e3 93.21%,
      #19bcfc 100.13%
    );
  background-origin: padding-box, border-box;
  background-clip: padding-box, border-box;

  &__labels {
    display: flex;
    width: 100%;
    justify-content: space-between;
    font-weight: bold;
    gap: em(16);
  }

  &__label {
    width: 25%;
    display: flex;
    flex-direction: column;
    white-space: nowrap;
    font-size: em(13);

    &--sell {
      align-items: flex-end;
      color: #b00020;
    }
    &--buy {
      align-items: flex-start;
      color: #1b5e20;
    }

    &-price {
      margin-bottom: 0.25em;
    }
    &-currency {
      font-size: 0.75em;
      color: #000;
    }
  }

  &__bar {
    display: flex;
    height: em(12);
    width: 55%;
    overflow: hidden;
    border-radius: em(6);
    background-color: #eee;

    &-segment {
      &--sell {
        background-color: #d32f2f;
        border-top-left-radius: em(6);
        border-bottom-left-radius: em(6);
      }
      &--gap {
        display: block;
        width: em(4);
        height: em(4);
        background-color: #fff;
      }
      &--buy {
        background-color: #388e3c;
        border-top-right-radius: em(6);
        border-bottom-right-radius: em(6);
      }
    }
  }
}
