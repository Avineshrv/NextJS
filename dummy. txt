import React from 'react';
import './fx-polls.scss';

const FxPollsBar = ({ sellPercent = 29, buyPercent = 71 }) => {
  return (
    <div className="fx-polls">
      <div className="fx-polls__labels">
        <div className="fx-polls__label fx-polls__label--sell">
          Selling {sellPercent}% <span>USD</span>
        </div>
        <div className="fx-polls__label fx-polls__label--buy">
          {buyPercent}% Buying <span>USD</span>
        </div>
      </div>
      <div className="fx-polls__bar">
        <div
          className="fx-polls__bar-segment fx-polls__bar-segment--sell"
          style={{ width: `${sellPercent}%` }}
        />
        <div
          className="fx-polls__bar-segment fx-polls__bar-segment--buy"
          style={{ width: `${buyPercent}%` }}
        />
      </div>
    </div>
  );
};

export default FxPollsBar;


.fx-polls {
  border: 1px solid #dcdcdc;
  border-radius: 1rem;
  padding: 1rem;
  width: 100%;
  background: white;

  &__labels {
    display: flex;
    justify-content: space-between;
    font-weight: bold;
    margin-bottom: 0.5rem;
  }

  &__label {
    font-size: 1rem;

    span {
      display: block;
      font-weight: normal;
      font-size: 0.875rem;
    }

    &--sell {
      color: #b00020;
    }

    &--buy {
      color: #1b5e20;
      text-align: right;
    }
  }

  &__bar {
    display: flex;
    height: 12px;
    width: 100%;
    overflow: hidden;
    border-radius: 6px;
    background-color: #eee;
  }

  &__bar-segment {
    height: 100%;

    &--sell {
      background-color: #d32f2f;
      border-top-left-radius: 6px;
      border-bottom-left-radius: 6px;
    }

    &--buy {
      background-color: #388e3c;
      border-top-right-radius: 6px;
      border-bottom-right-radius: 6px;
    }
  }
}
